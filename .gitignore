# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Test output
coverage

# Environment. Firebase web config is public by design, but a personal
# project's values should not be committed - see .env.example.
.env
.env.*
!.env.example

# Firebase tooling
.firebase/
firebase-debug.log
firestore-debug.log
database-debug.log
ui-debug.log
pubsub-debug.log

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Private planning docs - roadmaps, audits, competitive research.
# Never published: the public repo shows the product, not the plan.
docs/private/

# Local editor and tooling state.
.claude/

# The exported GTFS feed, and the operator inputs it is built from.
#
# The feed is build output. The config carries surveyed stop coordinates and
# the operator's own identity - data belonging to somebody else, and the one
# thing a fixture version of could be mistaken for the real survey.
dist-gtfs/
gtfs.config.json
